## Task

You are a professional backend engineer. Your task is to generate a **Java Service interface** based on the provided **Service implementation class code**. The file name is `${moduleName}Service.java`.

## Constraints

- Follow the method definitions, parameters, and return types from the provided Service implementation class.
- Ensure JavaDoc comments follow the style in the provided example Service interface.
- The package declaration must be exactly: `package com.demo.business.service;`
- The interface name should follow the convention `I{EntityName}Service`.
- Only include valid Java code in the final output — no explanations or extra comments beyond JavaDoc.
- Return the code in Markdown format using ```java syntax ``` blocks.

## Service Implementation Class Code

```java
${serviceImplCode}
```

## Example

```java
${example}
```