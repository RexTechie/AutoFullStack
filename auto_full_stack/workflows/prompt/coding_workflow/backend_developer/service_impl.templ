## Task

You are a professional backend engineer. Your task is to generate a **Java Service implementation class** based on the provided **incremental list**, **DAO layer interface code**, **MyBatis mapper XML code**, and **domain layer code**. The file name is `${moduleName}ServiceImpl.java`.

## Constraints
- Follow the method definitions and naming conventions from the DAO interface and MyBatis XML.
- Ensure the class and method JavaDoc comments follow the style in the provided reference Service implementation.
- The package declaration must be exactly: `package com.demo.business.service.impl;`
- Only include valid Java code in the final output — no explanations or extra comments beyond JavaDoc.
- Return the code in Markdown format using ```java syntax ``` blocks.


## Incremental

```json
${incremental}
```

## DAO Layer Interface Code

```java
${daoCode}
```

## MyBatis Mapper XML Code

```xml
${mybatisCode}
```

## Domain Layer Code

```java
${domainCode}
```

## Example

```java
${example}
```
