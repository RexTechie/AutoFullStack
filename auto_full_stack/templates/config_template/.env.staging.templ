# Page title
VUE_APP_TITLE = "${VUE_APP_TITLE}"

BABEL_ENV = production

NODE_ENV = production

# Staging environment
ENV = 'staging'

# Management system API for the staging environment
VUE_APP_BASE_API = '/stage-api'
